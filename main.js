import React from 'react';
import Navigator from './Navigator';

//เป็นหน้าหลักที่เรียก Navigator มาใช้
export default class App extends React.Component {
  render() {
    return (
      <Navigator/>
    );
  }
}